import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AddLyricToDb = () => {
  return (
    <View style={styles.container}>
      <Text>AddLyricToDb</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default AddLyricToDb;